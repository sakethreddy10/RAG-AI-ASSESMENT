# ============================================================
# Python Dependencies for the Document Q&A Bot (RAG System)
# ============================================================

# --- Web Framework ---
fastapi>=0.115.0          # Backend API framework (0.115+ supports newer starlette)
uvicorn[standard]==0.30.1 # ASGI server to run FastAPI

# --- Data Validation ---
pydantic>=2.9.0           # Request/response models for FastAPI

# --- Google Gemini ---
# Provides both the LLM (gemini-2.5-flash) and embedding model (text-embedding-004)
google-generativeai>=0.7.2

# --- Protobuf: pinned to be compatible with google-generativeai ---
# chromadb's opentelemetry wants >=5, but google-ai-generativelanguage needs <5
# Pinning <5 here because our code depends on google-generativeai working correctly.
protobuf>=3.19.5,<5.0.0dev

# --- Vector Database ---
# Real ChromaDB (disk-persistent). Replaces the old custom NumPy store.
chromadb>=0.4.22

# --- Document Parsing ---
pypdf>=4.0.0              # Extract text from PDF files, page by page
python-docx>=1.1.0        # Extract text from Microsoft Word (.docx) files

# --- Utilities ---
python-dotenv>=1.0.1      # Load API keys from a .env file
tqdm>=4.66.0              # Progress bars during document ingestion
aiofiles>=23.2.1          # Async file serving for FastAPI static files

# --- Optional: Web UI (Bonus) ---
streamlit>=1.30.0         # Simple browser-based UI (alternative to the CLI)
